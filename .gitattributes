# 链接器脚本必须固定 LF：Windows 检出若变成 CRLF，rust-lld 解析会报
# "expected filename pattern"（见 README 工具链注意事项）
*.x text eol=lf

# 手册 PDF 不入库:体量大(GitHub LFS 免费额度 1GB 存储/1GB 月带宽,推不动),
# 改在 docs/README.md 放官方下载链接(2026-08-30 从历史中整体摘除,
# 含 513a147 的 LFS 跟踪)。
